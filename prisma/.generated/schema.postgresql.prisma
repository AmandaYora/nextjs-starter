generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // overwritten by scripts/run-prisma.ts
  url      = env("DATABASE_URL")
}

enum Role {
  ADMIN
  USER
}

model User {
  id           String   @id @default(cuid())
  name         String
  email        String   @unique
  passwordHash String
  role         Role     @default(USER)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model AuditLog {
  id        String   @id @default(cuid())
  actorId   String
  action    String
  targetId  String
  metadata  String?
  createdAt DateTime @default(now())
}
